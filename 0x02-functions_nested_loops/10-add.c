#include "main.h"
/**
 * add - add two integers and returns the result
 * @n: int to be added c
 * @c: int to be added n
 * Return: sum or n and c
 */
int add(int n, int c)
{
	int l;

	l = n + c;

	return (l);
}
