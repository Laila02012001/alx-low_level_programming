#include "main.h"
#include <string.h>

/**
 * _strlen - return the length of a string
 * @s: integer
 * Return: return lenght of a string
 */
int _strlen(char *s)
{
	int l;

	l = strlen(s);
	return (l);
}
